import React from 'react';
import LoginPage from "@/app/(auth)/login/page";

const Home: React.FC = () => {
    return (
        <>
            <LoginPage />
        </>
    );
};

export default Home;
